namespace Coleções
{
    public class Ex01Boson
    {
        public static void Main23(string[] args)
        {   
            // System.Console.WriteLine("Digite a temperatura em graus Celsius para verificar se a água está no estado liquido ou gasoso: ");
            // decimal temperatura = System.Convert.ToDecimal(Console.ReadLine());
            // bool temperaturaInvalida;
            // temperaturaInvalida = (temperatura < 0 ) || (temperatura > 100);
            // Console.WriteLine(temperaturaInvalida);
            

            // System.Console.WriteLine("Digite a sua idade: ");
            // int idade = System.Convert.ToInt32(Console.ReadLine());
            // bool idadeValida;
            // idadeValida = (idade > 0) && (idade <= 120);
            // Console.WriteLine("Sua idade é: " + idadeValida);

            // bool valorLogico;
            // valorLogico = true;
            // valorLogico = !valorLogico;
            // Console.WriteLine(valorLogico);
            // Console.ReadLine();


        }
    }
}